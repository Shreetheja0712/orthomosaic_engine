# --- Ingestion ---
ExifRead>=3.0.0
pyexiftool>=0.5.6          # Sequoia XMP / radiometric fields (Stage 12)

# --- Feature extraction & matching (Stage 3 & 4) ---
torch>=2.2.0               # ALIKED + LightGlue backbone
torchvision>=0.17.0        # image loading utilities used by lightglue
lightglue>=0.1.0           # ALIKED extractor + LightGlue matcher (ETH Zurich)
kornia>=0.7.3              # image geometry utils required by lightglue
h5py>=3.10.0               # .h5 feature / match files written by extractor + matcher

# --- SfM (Stage 6 + 7) ---
pycolmap>=4.0.4; python_version < "3.14"

# --- Geospatial / raster I/O (Stages 7-13) ---
rasterio>=1.3.0
GDAL>=3.8.0                # fillnodata, warp, GeoTIFF internals
pyproj>=3.6.0              # CRS transforms

# --- Depth / DSM (Stage 8 + 9) ---
# OpenMVS: build from source or  conda install -c conda-forge openmvs
scipy>=1.12.0              # DSM gap fill fallback

# --- Orthorectification (Stage 10) ---
cupy-cuda12x>=13.0.0       # GPU backward projection (requires CUDA 12.x)
numpy>=1.26.0

# --- Mosaicking (Stage 11 + 12) ---
opencv-python>=4.9.0       # GainCompensator, GraphCutSeamFinder, MultiBandBlender, ArUco

# --- Testing ---
pytest>=9.0.0
